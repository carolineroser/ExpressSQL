extends ../layout

block content
  h1= title
  p Welcome to #{title}
  form(action="/lead/" + lead.id + "/edit", method="POST")
    input(name="lead_email", type="email", placeholder="Enter email address", value=lead.email)
    button(type="submit") Save